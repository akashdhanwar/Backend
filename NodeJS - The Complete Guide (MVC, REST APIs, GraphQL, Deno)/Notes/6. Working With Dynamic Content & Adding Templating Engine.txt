(Lec-79) - Module Introduction
(Lec-80) - Sharing Data Across Requests and Users
    Use variables to store data

    const products = [];
    // /admin/add-product => POST
    router.post("/add-product", (req, res, next) => {
      products.push({ title: req.body.title });
      res.redirect("/");
    });

    We can export it from here and use the variable in other file
    It will show the updated value, even on other Browser
    The data is inherent to Node server

(Lec-81) - Templating Engines - An Overview
    For Dynamic Content
    HTMLish Template with some blanks/placeholders
    We also have Node/Express Content in our app like products 
    We have a Templating Engine that understands a certain syntax and scans HTMLish Template
    Then, replaces placeholders/snippets with HTML Content
    And then sends back the HTML file

    Available Templating Engines - 
    EJS             -   <p><%= name %></p>  -   Use normal HTML and plain JS in your templates
    Pug(Jade)       -   p #{name}           -   Use minimal HTML and custom template language
    Handlebars      -   <p>{{name}}</p>     -   Use normal HTML and custom template language

(Lec-82) - Installing and Implementing Pug
    npm i --save ejs pug express-handlebars
    app.set() allows us to set global values on our express application, like view and 'view engine'

    app.set('view engine', 'pug');
    app.set('views', 'views');

    Add a pug template in views - shop.pug

    router.get("/", (req, res, next) => {
      res.render("shop");
    });

(Lec-83) - Outputting Dynamic Content
(Lec-84) - Official Pug Docs
    https://pugjs.org/api/getting-started.html

(Lec-85) - Converting HTML Files to Pug
(Lec-86) - Adding a Layout
    Add layouts, hook parts using block
    extends filepath
    block styles

(Lec-87) - Finishing the Pug Template
(Lec-88) - Avoiding an Error
    npm install --save express-handlebars@3.0

(Lec-89) - Working With Handlebars
    Engine name we define should match with engine name and the extension of file

(Lec-90) - Converting our Project to Handlebars
(Lec-91) - Adding the Layouts to Handlebars
(Lec-92) - Working With EJS
    We will be using in course

(Lec-93) - Working on the Layout with Partials/includes
(Lec-94) - Wrap Up 
    We render template by using special render method provided by Express
    It looks for the registered view engine in app.js
    It then loads the file with the configurations like variables passed in render

#### Assignment 4: Time to Practice - Templating Engines
    Create a npm project and install Express and Nodemon, and also install a templating engine
    Add two routes  -   '/' holds a <form> that allows users to input their name
                        '/users' Outputs an <ul> with the user name (or some error text)
    Use layouts/includes

(Lec-95) - [OPTIONAL] Assignment Solution
(Lec-96) - Useful Resources & Links 
    Pug Docs: https://pugjs.org/api/getting-started.html
    Handlebars Docs: https://handlebarsjs.com/
    EJS Docs: http://ejs.co/#docs
